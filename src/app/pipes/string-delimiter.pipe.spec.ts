import { StringDelimiterPipe } from './string-delimiter.pipe';

describe('StringDelimiterPipe', () => {
  it('create an instance', () => {
    const pipe = new StringDelimiterPipe();
    expect(pipe).toBeTruthy();
  });
});
